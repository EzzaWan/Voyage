<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your eSIM is Ready</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #E9F1FF;
      background-color: #0A1A2F;
      margin: 0;
      padding: 0;
    }
    .wrapper {
      width: 100%;
      background-color: #0A1A2F;
      padding: 40px 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #132742;
      border-radius: 16px;
      overflow: hidden;
      border: 1px solid #1E3555;
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }
    .header {
      background-color: #0F2540;
      padding: 30px;
      text-align: center;
      border-bottom: 1px solid #1E3555;
    }
    .logo {
      font-size: 26px;
      font-weight: 800;
      color: #1E90FF;
      text-decoration: none;
      letter-spacing: -0.5px;
    }
    .content {
      padding: 40px 30px;
    }
    h1 {
      margin: 0 0 10px;
      font-size: 24px;
      font-weight: 700;
      color: #FFFFFF;
      text-align: center;
    }
    .subtitle {
      text-align: center;
      color: #A8B5C8;
      font-size: 16px;
      margin-bottom: 30px;
    }
    .info-box {
      background-color: #0A1A2F;
      border: 1px solid #1E3555;
      border-radius: 12px;
      padding: 20px;
      margin-bottom: 24px;
    }
    .info-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 12px;
      border-bottom: 1px solid #1E3555;
      padding-bottom: 12px;
    }
    .info-row:last-child {
      margin-bottom: 0;
      border-bottom: none;
      padding-bottom: 0;
    }
    .label {
      color: #A8B5C8;
      font-size: 14px;
    }
    .value {
      color: #FFFFFF;
      font-weight: 600;
      font-size: 14px;
      text-align: right;
    }
    .action-button {
      display: block;
      width: 100%;
      background-color: #1E90FF;
      color: #FFFFFF;
      text-decoration: none;
      padding: 16px 0;
      border-radius: 8px;
      font-weight: 600;
      text-align: center;
      margin: 20px 0;
      box-shadow: 0 4px 12px rgba(30, 144, 255, 0.3);
    }
    .secondary-button {
      display: block;
      width: 100%;
      background-color: transparent;
      border: 1px solid #1E3555;
      color: #E9F1FF;
      text-decoration: none;
      padding: 14px 0;
      border-radius: 8px;
      font-weight: 600;
      text-align: center;
      margin: 10px 0;
    }
    .footer {
      padding: 30px;
      text-align: center;
      border-top: 1px solid #1E3555;
      color: #64748B;
      font-size: 12px;
    }
    .status-badge {
      display: inline-block;
      background-color: #10B981;
      color: white;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 12px;
      font-weight: 600;
      margin-bottom: 15px;
    }
    .instructions-box {
      background-color: #0F2540;
      border: 1px solid #1E3555;
      border-radius: 12px;
      padding: 24px;
      margin: 24px 0;
    }
    .instructions-title {
      color: #1E90FF;
      font-size: 16px;
      font-weight: 700;
      margin-bottom: 16px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .step {
      display: flex;
      margin-bottom: 16px;
    }
    .step-number {
      background-color: #1E90FF;
      color: white;
      width: 24px;
      height: 24px;
      border-radius: 50%;
      text-align: center;
      line-height: 24px;
      font-size: 12px;
      font-weight: 700;
      margin-right: 12px;
      flex-shrink: 0;
    }
    .step-text {
      color: #E9F1FF;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="container">
      <div class="header">
        <div class="logo">Voyo</div>
      </div>
      
      <div class="content">
        <div style="text-align: center;">
          <span class="status-badge">Ready to Activate</span>
        </div>
        <h1>Your eSIM is Ready!</h1>
        <p class="subtitle">Great news, {{user.name}}! Your profile is ready.</p>
        
        <div class="info-box">
          <div class="info-row">
            <span class="label">Plan</span>
            <span class="value">{{plan.name}}</span>
          </div>
          <div class="info-row">
            <span class="label">ICCID</span>
            <span class="value" style="font-family: monospace;">{{profile.iccid}}</span>
          </div>
          <div class="info-row">
            <span class="label">Status</span>
            <span class="value" style="color: #10B981;">{{profile.esimStatus}}</span>
          </div>
          {{#if profile.totalVolume}}
          <div class="info-row">
            <span class="label">Data Volume</span>
            <span class="value">{{profile.totalVolume}}</span>
          </div>
          {{/if}}
          {{#if profile.expiredTime}}
          <div class="info-row">
            <span class="label">Expires</span>
            <span class="value">{{profile.expiredTime}}</span>
          </div>
          {{/if}}
        </div>

        {{#if profile.qrCodeUrl}}
        <div style="text-align: center; margin: 30px 0;">
          <a href="{{profile.qrCodeUrl}}" class="action-button">Download QR Code</a>
        </div>
        {{/if}}

        <div class="instructions-box">
          <div class="instructions-title">Installation Instructions</div>
          <div class="step">
            <div class="step-number">1</div>
            <div class="step-text">Open <strong>Settings</strong> on your device</div>
          </div>
          <div class="step">
            <div class="step-number">2</div>
            <div class="step-text">Go to <strong>Cellular/Mobile Data</strong> &rarr; <strong>Add eSIM</strong></div>
          </div>
          <div class="step">
            <div class="step-number">3</div>
            <div class="step-text">Scan the QR code provided</div>
          </div>
          <div class="step">
            <div class="step-number">4</div>
            <div class="step-text">Follow the on-screen prompts to activate</div>
          </div>
        </div>

        {{#if receiptDownloadUrl}}
          <a href="{{receiptDownloadUrl}}" class="secondary-button">Download Receipt</a>
        {{/if}}
      </div>

      <div class="footer">
        <p style="margin: 0 0 10px;">This is an automated notification from Voyo.</p>
        <p style="margin: 0;">&copy; 2024 Voyo. All rights reserved.</p>
      </div>
    </div>
  </div>
</body>
</html>
