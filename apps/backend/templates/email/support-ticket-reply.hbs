<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Support Ticket Reply</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      line-height: 1.6;
      color: #E9F1FF;
      background-color: #0A1A2F;
      margin: 0;
      padding: 0;
    }
    .wrapper {
      width: 100%;
      background-color: #0A1A2F;
      padding: 40px 0;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      background-color: #132742;
      border-radius: 16px;
      overflow: hidden;
      border: 1px solid #1E3555;
      box-shadow: 0 4px 20px rgba(0,0,0,0.3);
    }
    .header {
      background-color: #0F2540;
      padding: 30px;
      text-align: center;
      border-bottom: 1px solid #1E3555;
    }
    .logo {
      font-size: 26px;
      font-weight: 800;
      color: #1E90FF;
      text-decoration: none;
      letter-spacing: -0.5px;
    }
    .content {
      padding: 40px 30px;
    }
    h1 {
      margin: 0 0 10px;
      font-size: 24px;
      font-weight: 700;
      color: #FFFFFF;
      text-align: center;
    }
    .subtitle {
      text-align: center;
      color: #A8B5C8;
      font-size: 16px;
      margin-bottom: 30px;
    }
    .info-box {
      background-color: #0A1A2F;
      border: 1px solid #1E3555;
      border-radius: 12px;
      padding: 20px;
      margin-bottom: 24px;
    }
    .info-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 12px;
      border-bottom: 1px solid #1E3555;
      padding-bottom: 12px;
    }
    .info-row:last-child {
      margin-bottom: 0;
      border-bottom: none;
      padding-bottom: 0;
    }
    .label {
      color: #A8B5C8;
      font-size: 14px;
    }
    .value {
      color: #FFFFFF;
      font-weight: 600;
      font-size: 14px;
      text-align: right;
    }
    .section-label {
      color: #A8B5C8;
      font-size: 12px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 10px;
    }
    .message-bubble {
      background-color: #0F2540;
      border: 1px solid #1E3555;
      border-radius: 8px;
      padding: 20px;
      color: #A8B5C8;
      font-size: 14px;
      white-space: pre-wrap;
      margin-bottom: 24px;
    }
    .reply-bubble {
      background-color: rgba(30, 144, 255, 0.1);
      border: 1px solid #1E90FF;
      border-radius: 8px;
      padding: 20px;
      color: #E9F1FF;
      font-size: 15px;
      white-space: pre-wrap;
      box-shadow: 0 2px 8px rgba(30, 144, 255, 0.1);
    }
    .footer {
      padding: 30px;
      text-align: center;
      border-top: 1px solid #1E3555;
      color: #64748B;
      font-size: 12px;
    }
    .ticket-link {
      display: inline-block;
      margin-top: 20px;
      color: #1E90FF;
      text-decoration: none;
      font-weight: 600;
    }
  </style>
</head>
<body>
  <div class="wrapper">
    <div class="container">
      <div class="header">
        <div class="logo">Voyo</div>
      </div>
      
      <div class="content">
        {{#if adminReply}}
          <h1>Support Team Reply</h1>
          <p class="subtitle">Our team has responded to your ticket.</p>
        {{else}}
          <h1>Customer Reply</h1>
          <p class="subtitle">A customer has replied to a ticket.</p>
        {{/if}}
        
        <div class="info-box">
          <div class="info-row">
            <span class="label">Ticket ID</span>
            <span class="value">{{ticketId}}</span>
          </div>
          <div class="info-row">
            <span class="label">Customer</span>
            <span class="value">{{customerName}}</span>
          </div>
          {{#if customerEmail}}
          <div class="info-row">
            <span class="label">Email</span>
            <span class="value">{{customerEmail}}</span>
          </div>
          {{/if}}
        </div>

        <div class="section-label">Original Message</div>
        <div class="message-bubble">{{ticketMessage}}</div>

        {{#if adminReply}}
          <div class="section-label" style="color: #1E90FF;">Voyo Support Response</div>
          <div class="reply-bubble">{{adminReply}}</div>
        {{else}}
          <div class="section-label" style="color: #1E90FF;">Customer Response</div>
          <div class="reply-bubble">{{customerReply}}</div>
        {{/if}}
      </div>

      <div class="footer">
        <p style="margin: 0 0 10px;">This is an automated notification from the Voyo Support System.</p>
        {{#if customerEmail}}
          <p style="margin: 0;">Please respond to the customer at: <a href="mailto:{{customerEmail}}" style="color: #1E90FF; text-decoration: none;">{{customerEmail}}</a></p>
        {{else}}
           <p style="margin: 0;">You can view and reply to this ticket in your dashboard.</p>
        {{/if}}
      </div>
    </div>
  </div>
</body>
</html>
